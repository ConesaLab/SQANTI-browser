"""Bundled Claude Agent Skill for SQANTI-browser.

The skill itself lives in ``src/skills/data/`` (SKILL.md + references + assets).
``install.py`` copies that directory into the user's Claude skills directory so an
agent (e.g. Claude Code) can drive SQANTI-browser correctly.
"""
